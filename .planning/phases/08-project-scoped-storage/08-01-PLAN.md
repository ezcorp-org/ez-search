---
phase: 08-project-scoped-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/paths.ts
  - src/services/manifest-cache.ts
  - src/cli/commands/index-cmd.ts
  - src/cli/commands/status-cmd.ts
  - src/services/vector-db.ts
autonomous: true

must_haves:
  truths:
    - "Index data (col-768, col-512, schema-version.json, manifest.json) lives at <project>/.ez-search/"
    - "Manifest lives at <project>/.ez-search/manifest.json instead of <project>/.ez-search-cache"
    - "Shared model weights remain at ~/.ez-search/models/ (unchanged)"
    - "ez-search index . creates .ez-search/ directory inside the project"
    - "ez-search index --clear . removes .ez-search/ directory and recreates it"
    - "ez-search status shows the new storage path"
    - "ez-search query still loads manifest from the correct location"
    - ".ez-search/ is already in BUILTIN_EXCLUSIONS so the scanner never indexes its own data"
  artifacts:
    - path: "src/config/paths.ts"
      provides: "resolveProjectStoragePath returns <projectDir>/.ez-search/"
      contains: "path.join.*\\.ez-search"
    - path: "src/services/manifest-cache.ts"
      provides: "Manifest stored at .ez-search/manifest.json via resolveProjectStoragePath"
      contains: "manifest.json"
    - path: "src/cli/commands/index-cmd.ts"
      provides: "Simplified --clear (no redundant clearManifest call)"
    - path: "src/cli/commands/status-cmd.ts"
      provides: "manifestPath uses resolveProjectStoragePath + MANIFEST_FILENAME"
  key_links:
    - from: "src/services/manifest-cache.ts"
      to: "src/config/paths.ts"
      via: "import resolveProjectStoragePath"
      pattern: "resolveProjectStoragePath"
    - from: "src/services/vector-db.ts"
      to: "src/config/paths.ts"
      via: "import resolveProjectStoragePath"
      pattern: "resolveProjectStoragePath"
    - from: "src/cli/commands/status-cmd.ts"
      to: "src/config/paths.ts"
      via: "import resolveProjectStoragePath for manifest path"
      pattern: "resolveProjectStoragePath"
---

<objective>
Move per-project index storage from `~/.ez-search/<basename>-<hash>/` into `<project>/.ez-search/` and relocate the manifest from `<project>/.ez-search-cache` to `<project>/.ez-search/manifest.json`.

Purpose: Project-scoped storage makes indexes discoverable — delete the project, delete the index. Consistent with `.git/`, `.next/`, etc.

Output: All index data (vectors, manifest, schema version) stored inside the project directory at `.ez-search/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/config/paths.ts
@src/services/manifest-cache.ts
@src/services/vector-db.ts
@src/cli/commands/index-cmd.ts
@src/cli/commands/query-cmd.ts
@src/cli/commands/status-cmd.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor core path and manifest modules</name>
  <files>src/config/paths.ts, src/services/manifest-cache.ts</files>
  <action>
**src/config/paths.ts:**

Change `resolveProjectStoragePath()` to return `<projectDir>/.ez-search/` instead of `~/.ez-search/<basename>-<hash>/`.

The new implementation:
```ts
export function resolveProjectStoragePath(projectDir: string): string {
  return path.join(path.resolve(projectDir), '.ez-search');
}
```

Remove the `crypto` import (no longer needed). Keep `os` import (used by `resolveModelCachePath`). Update JSDoc to: `Format: <projectDir>/.ez-search/`

`resolveModelCachePath()` stays unchanged (still returns `~/.ez-search/models/`).

**src/services/manifest-cache.ts:**

1. Change `MANIFEST_FILENAME` from `'.ez-search-cache'` to `'manifest.json'`.

2. Add import: `import { resolveProjectStoragePath } from '../config/paths.js';`

3. Add `mkdirSync` to the existing `fs` imports.

4. Create a helper to compute manifest path (DRY):
   ```ts
   function manifestPath(projectDir: string): string {
     return path.join(resolveProjectStoragePath(projectDir), MANIFEST_FILENAME);
   }
   ```

5. Update `loadManifest()` to use `manifestPath(projectDir)` instead of `path.join(projectDir, MANIFEST_FILENAME)`.

6. Update `saveManifest()`:
   - Use `manifestPath(projectDir)` for the file path
   - Add `mkdirSync(resolveProjectStoragePath(projectDir), { recursive: true })` before writing (ensures .ez-search/ dir exists on first save)
   - `tmpPath = filePath + '.tmp'` still works (now writes inside .ez-search/)

7. Update `clearManifest()` to use `manifestPath(projectDir)`.

8. Update module-level JSDoc to mention the new location.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Grep for `.ez-search-cache` across src/ to confirm no stale references.
  </verify>
  <done>
`resolveProjectStoragePath()` returns `<projectDir>/.ez-search/`. Manifest functions read/write `<projectDir>/.ez-search/manifest.json`. No stale `.ez-search-cache` references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update consumer modules and comments</name>
  <files>src/cli/commands/index-cmd.ts, src/cli/commands/status-cmd.ts, src/services/vector-db.ts</files>
  <action>
**src/cli/commands/index-cmd.ts (LOGIC CHANGE):**

In the `--clear` block, remove the `clearManifest(absPath)` call. After the refactor, `storagePath` IS `<project>/.ez-search/` which contains `manifest.json`, so `rmSync(storagePath, { recursive: true, force: true })` already deletes everything including the manifest. The `clearManifest` call is redundant.

Specifically: find the `if (options.clear)` block and remove the two lines that import and call `clearManifest`. The block should just do `rmSync(storagePath, ...)` then `openProjectCollections(absPath)`. Keep all other imports (`loadManifest`, `saveManifest`, `hashContent`, `hashText`, `makeChunkId`) in their existing destructured import — they are still used.

**src/cli/commands/status-cmd.ts (LOGIC CHANGE — CRITICAL):**

IMPORTANT: Task 1 changes `MANIFEST_FILENAME` from `'.ez-search-cache'` to `'manifest.json'`. The existing code `path.join(projectDir, MANIFEST_FILENAME)` would produce `<projectDir>/manifest.json` which is WRONG — manifest now lives at `<projectDir>/.ez-search/manifest.json`.

Fix the manifest existence check: replace the `manifestPath` construction to use `resolveProjectStoragePath`:
```ts
const { resolveProjectStoragePath } = await import('../../config/paths.js');
const { MANIFEST_FILENAME, loadManifest } = await import('../../services/manifest-cache.js');
const manifestPath = path.join(resolveProjectStoragePath(projectDir), MANIFEST_FILENAME);
```

This dynamic import of `resolveProjectStoragePath` is consistent with the existing pattern in the same file (it already dynamically imports from paths.js later for the storage path check). No other changes needed — `loadManifest(projectDir)` resolves the path internally.

**src/services/vector-db.ts (DOCUMENTATION ONLY — NO LOGIC CHANGES):**

Update JSDoc comments only:
- Module-level comment: change `~/.ez-search/<project>-<hash>/` references to `<project>/.ez-search/`
- `openProjectCollections` JSDoc: update storage layout to `<projectDir>/.ez-search/col-768/` and `<projectDir>/.ez-search/col-512/`

**src/cli/commands/query-cmd.ts (NO CHANGES NEEDED):**

`loadManifest(projectDir)` resolves the manifest path internally via `resolveProjectStoragePath`. No code or documentation changes required.

**Final sweep:** Grep `src/` for:
- `.ez-search-cache` — should have ZERO hits
- `homedir()` in paths.ts — should only appear in `resolveModelCachePath`
  </action>
  <verify>
Run `npx tsc --noEmit` for full type check. Grep `src/` for `.ez-search-cache` to confirm zero hits. Build with `npx tsc` to confirm dist/ output compiles cleanly.
  </verify>
  <done>
All consumer modules updated. index-cmd --clear simplified (no redundant clearManifest). status-cmd manifest path check uses resolveProjectStoragePath + MANIFEST_FILENAME for correct path. vector-db comments updated. Zero references to `.ez-search-cache` or old hash-based paths remain in src/.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npx tsc` builds successfully to dist/
3. No references to `.ez-search-cache` anywhere in `src/`
4. No references to the old `~/.ez-search/<basename>-<hash>/` pattern in code
5. `resolveProjectStoragePath('/tmp/test')` returns `/tmp/test/.ez-search`
6. `resolveModelCachePath()` still returns `~/.ez-search/models/` (unchanged)
7. **E2E smoke test:** Create a temp dir, run `ez-search index . --format text`, confirm `.ez-search/` directory created with `col-768/`, `col-512/`, `manifest.json` inside it
8. **Query smoke test:** Run `ez-search query "test" --format text` in the same dir, confirm it finds results (manifest loaded from correct location)
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- All index data paths resolve to `<project>/.ez-search/`
- Manifest path resolves to `<project>/.ez-search/manifest.json`
- Model cache path unchanged at `~/.ez-search/models/`
- No stale path references in source code
- --clear flag still works (rmSync handles everything)
- E2E: index creates .ez-search/ in project, query reads from it
</success_criteria>

<output>
After completion, create `.planning/phases/08-project-scoped-storage/08-01-SUMMARY.md`
</output>
